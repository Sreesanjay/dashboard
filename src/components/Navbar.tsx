import { StyledHeader } from "../styles/Header.styled";

const Navbar = () => {
     return (
          <StyledHeader>
               <h1>Dashbord</h1>
          </StyledHeader>
     );
};

export default Navbar;

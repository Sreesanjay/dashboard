export interface IStudent {
    id: string;
    name: string;
    age: number;
    place: string;
    email: string;
    phone: string;
    education: string;
}

export interface ITheme {
    name: string
    backgroundColor: string;
    color: string;
    secondaryColor: string;
}